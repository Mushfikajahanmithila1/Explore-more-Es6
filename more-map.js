const numvbers = [12, 56, 47, 87, 69];

const double = numvbers.map((num) => num * 2);
// console.log(double);

const friends = ["BMW", "Marchirise", "Lamborgini", "Tesla", "Royels-Rolls"];

const friend = friends.map((n) => n[0]);
const length = friends.map((frie) => frie.length);
console.log(friend, length);
